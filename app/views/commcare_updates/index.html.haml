.row
  .col-md-12
    %ol.breadcrumb
      %li.active
        = fa_icon "home"
        Liste des mises à jour

.row
  .col-md-12
    %p.text-right
      %a.btn.btn-primary{href: new_commcare_update_path, role: "button"}
        Nouvelle mise à jour
        = fa_icon "plus"

    %table.table
      %thead
        %tr
          %th Date
          %th Etape
          %th Action
      - @commcare_updates.each do |u|
        %tr{class: status_class(u)}
          %td= date_label u
          %td= progress_label u
          %td= action_link(u)

    %div.text-center= will_paginate @updates, renderer: BootstrapPagination::Rails, next_label: "Suiv. »", previous_label: "« Préc."
